resources:
  jobs:
    foundation_models_overview_job:
      name: foundation_models_overview
      tags:
        project_name: "arxiv-curator"

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
            dependencies:
              - ../dist/*.whl

      tasks:
        - task_key: foundation_models_overview
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/1.1_foundation_models_overview.py
            base_parameters:
              env: ${bundle.target}
              git_sha: "${var.git_sha}"
              run_id: "{{job.run_id}}"
